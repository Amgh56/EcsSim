package university.susu;

import university.University;

/** so this is like susu that makes the university always busy with events */
public interface Events {

  void eventsDuringYear(University university);

  void budgetThreaten(University university);
}
